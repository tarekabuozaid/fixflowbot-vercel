{
  "name": "fixflowbot-vercel",
  "version": "1.0.0",
  "description": "Telegram bot for FixFlow integration",
  "type": "commonjs",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "prisma generate",
    "start": "vercel dev",
    "postinstall": "prisma generate",
    "vercel-build": "prisma generate",
    "seed": "node scripts/seed.js",
    "test": "node scripts/test-bot.js"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "node-fetch": "^3.3.2",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "prisma": "^5.10.0"
  }
}
