{
  "name": "fixflowbot-vercel",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "engines": { "node": ">=18" },
  "type": "commonjs",
  "scripts": {
    "set:webhook": "node scripts/setWebhook.js",
    "del:webhook": "node scripts/deleteWebhook.js",
    "diag": "node scripts/diag.js",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "telegraf": "^4.15.3",
    "@prisma/client": "^5.10.0"
  },
  "devDependencies": {
    "prisma": "^5.10.0"
  }
}
