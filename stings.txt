إليك إعدادات البيئة اللازمة لتشغيل بوت FixFlow بشكل صحيح، مع شرح للخطوات والأدوار الرئيسية لضمان عمله المستمر وفهم طريقة عمله:

## 🔧 ملف الإعدادات (.env)

قم بإنشاء ملف باسم `.env` داخل جذر المشروع واكتب المتغيرات التالية، ثم عيّن القيم الصحيحة لها:

```bash
# مفتاح البوت من BotFather
BOT_TOKEN=123456789:AA...     

# رابط نشر التطبيق (مثلاً على Vercel)
# يجب أن يكون بالرابط الكامل https://your-app.vercel.app بدون مسار فرعي
PUBLIC_URL=https://your-app.vercel.app

# رابط قاعدة البيانات Postgres (مع إعدادات التشفير والاتصال)
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require&connection_limit=5&pool_timeout=20"

# معرف الماستر (المالك الرئيسي للبوت – رقم حساب Telegram)
MASTER_ID=123456789

# (اختياري) معرّفات المشرفين الثانويين، مفصولة بفواصل
ADMIN_IDS=123456789,987654321
```

### ماذا تعني هذه المتغيرات؟

* **BOT\_TOKEN**: رمز المصادقة الذي تحصل عليه من @BotFather بعد إنشاء البوت. يجب أن يكون سرّيًا.
* **PUBLIC\_URL**: عنوان الويب الذي يستضيف البوت (أو endpoint في حال استخدام Vercel). Telegram يستخدمه عند إرسال الطلبات إلى البوت.
* **DATABASE\_URL**: سلسلة الاتصال بقاعدة بيانات PostgreSQL التي ستُخزن فيها بيانات المستخدمين والبلاغات.
* **MASTER\_ID**: رقم مستخدم Telegram للماستر الذي يوافق على المنشآت الجديدة ويدير النظام.
* **ADMIN\_IDS**: قائمة بمستخدمي Telegram الذين لديهم صلاحيات إضافية (يمكن تركه فارغًا).

## 📦 خطوات تشغيل البوت

1. **تثبيت التبعيات**:
   في مجلد المشروع، نفّذ:

   ```bash
   npm install
   npx prisma generate        # إنشاء Prisma Client من المخطط
   npx prisma migrate deploy  # إنشاء الجداول في قاعدة البيانات
   ```
2. **إعداد قاعدة البيانات**:
   تأكد من أن `DATABASE_URL` يشير إلى قاعدة بيانات تعمل ولديها صلاحيات إنشاء الجداول.
3. **إعداد الـWebhook** (في حال نشره على خادم مثل Vercel):
   بعد ضبط `PUBLIC_URL`, نفّذ:

   ```bash
   npm run webhook:set
   ```

   لتسجيل عنوان الـWebhook لدى Telegram.
4. **تشغيل البوت محليًا** (للاختبار باستخدام Long‑Polling):

   ```bash
   npm start
   ```
5. **قراءة السجلات ومراقبة الأخطاء**:
   أثناء التشغيل ستظهر رسائل في وحدة التحكم توضح حالة الاتصال أو أي أخطاء. استخدمها لتصحيح الإعدادات.

## 🚀 نبذة عن تدفق عمل البوت

عند تشغيل البوت، يستقبل المستخدمون رسالة الترحيب مع قائمة خيارات تختلف حسب دورهم. إذا لم يكن المستخدم مسجّلًا بعد، يمكنه تسجيل منشأة جديدة أو الانضمام كمستخدم عادي، ثم يمر بمراحل الموافقة. عند التسجيل، يجمع البوت معلومات المنشأة (اسم، مدينة، هاتف) ويطلب اختيار خطة اشتراك، ثم يُرسل طلب التفعيل إلى الماستر. بعد التفعيل، تظهر لوحة تحكم متكاملة تسمح للمستخدم بإنشاء بلاغات صيانة (Work Orders)، إدارة الأعضاء، واستعراض الإحصائيات وغيرها من الوظائف المتقدمة.

بهذا الإعداد والخطوات سيكون بإمكانك تشغيل البوت بكفاءة وحفظ ملف الإعدادات لضمان العمل المستمر وفهم طريقة التشغيل.
